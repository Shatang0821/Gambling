namespace Game.Event
{
    public enum InputEvents
    {
        OnHorizontal,
        OnVertical,
        OnAttack,
        OnDefence,
        OnDash,
        OnJump
    }

    public enum TitleEvents
    {
        OnClosePanel,
    }

}